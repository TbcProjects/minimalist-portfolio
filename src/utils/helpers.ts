// import  { PageContentData } from "@typesDef/general";

// import { IHomepageData } from "@app/page";
// import { ISiteLayoutData } from "@app/layout";

// const findContentByKey = (content: PageContentData, key: string) => {
//   return content?.find(
//     (item) => item._modelApiKey && item._modelApiKey === key
//   );
// };
